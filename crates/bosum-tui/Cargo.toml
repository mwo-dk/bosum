[package]
name = "bosum-tui"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Bosum: a Norton Commander style file manager for the terminal"

[[bin]]
name = "bosum"
path = "src/main.rs"

[dependencies]
bosum-core = { path = "../bosum-core" }
ratatui = "0.30"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
unicode-width = "0.2"
